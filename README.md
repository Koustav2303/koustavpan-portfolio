```markdown
# 🌐 KOUSTAV.DEV // DIGITAL_REALITY_ENGINE

![React](https://img.shields.io/badge/react-%2320232a.svg?style=for-the-badge&logo=react&logoColor=%2361DAFB)
![Vite](https://img.shields.io/badge/vite-%23646CFF.svg?style=for-the-badge&logo=vite&logoColor=white)
![TailwindCSS](https://img.shields.io/badge/tailwindcss-%2338B2AC.svg?style=for-the-badge&logo=tailwind-css&logoColor=white)
![Framer Motion](https://img.shields.io/badge/Framer_Motion-black?style=for-the-badge&logo=framer&logoColor=blue)
![Java](https://img.shields.io/badge/java-%23ED8B00.svg?style=for-the-badge&logo=openjdk&logoColor=white)
![Spring Boot](https://img.shields.io/badge/springboot-%236DB33F.svg?style=for-the-badge&logo=spring&logoColor=white)

> **WARNING:** This is not a static webpage. This is a highly-optimized, 60FPS interactive frontend system built to showcase advanced UI/UX engineering, physics-based animations, and complex client-side architecture.

🔗 **Initialize Live Deployment:** [Insert Your GitHub Pages URL Here]

---

## 📂 SYSTEM ARCHITECTURE & DIRECTORY MAP

The project is structured using a modular, highly scalable component-based architecture powered by React and Vite. 

```text
koustav-portfolio/
├── dist/                   # Compiled, minified production build
├── node_modules/           # System dependencies
├── public/                 # Static global assets
├── src/                    # Core source code
│   ├── assets/             # Local media, images, and downloadable resumes
│   │
│   ├── components/         # Reusable, isolated UI & logic modules
│   │   ├── CommandPalette.jsx # Ctrl+K global search & command interface
│   │   ├── CustomCursor.jsx   # Physics-based custom viewport pointer
│   │   ├── GithubGraph.jsx    # 2.5D holographic GitHub contribution matrix
│   │   ├── HexaBot.jsx        # Draggable client-side AI terminal
│   │   ├── Preloader.jsx      # Initial boot-sequence animation screen
│   │   ├── ScrollProgress.jsx # Dynamic viewport scroll indicator
│   │   ├── SoundManager.jsx   # Global audio context controller
│   │   ├── Testimonials.jsx   # Interactive client review carousel
│   │   └── ThemeToggle.jsx    # Cyber-curtain Dark/Light mode switcher
│   │
│   ├── data/               # Local data structures & state configs
│   │   └── botData.js         # NLP knowledge base and fallback arrays for HexaBot
│   │
│   ├── pages/              # High-level route components
│   │   ├── About.jsx          # Background, skills, and Multi-Threaded Terminal
│   │   ├── Contact.jsx        # Transmission and uplink forms
│   │   ├── Home.jsx           # Landing matrix and hero section
│   │   └── Projects.jsx       # Interactive grid of engineered applications
│   │
│   ├── App.css             # Global stylesheet cascades
│   ├── App.jsx             # Main router, layout wrapper, and feature activator
│   ├── index.css           # Tailwind directives and core CSS variables
│   └── main.jsx            # React DOM injection point
│
├── .gitignore              # Git exclusion rules
├── eslint.config.js        # Strict linting rules for code quality
└── index.html              # Core HTML template

```

---

## 🚀 CORE SUB-SYSTEMS

### 🧠 HexaBot v3.0 (Autonomous Client-Side Entity)

A fully draggable, integrated AI terminal (`src/components/HexaBot.jsx`) that processes natural language queries regarding professional background, strictly within the browser's memory using `src/data/botData.js`.

* **Heuristic Scoring Engine:** Bypasses basic keyword matching. It tokenizes user input, scores the array against a categorized knowledge base, and gracefully handles typos.
* **Physics-Bound UI:** Leverages `framer-motion` drag controls, allowing users to physically reposition the terminal across the DOM viewport.

### 📊 2.5D Holographic Data Matrix (GitHub Pipeline)

A cyberpunk-themed, multi-layered visualizer for GitHub contribution data (`src/components/GithubGraph.jsx`).

* **True Parallax Rendering:** Uses exact mouse coordinates mapped to `useSpring` hooks to calculate the physical tilt (`rotateX`, `rotateY`) of a glassmorphism chassis.
* **Opposing Grid Physics:** The background grid coordinates move inversely to the mouse vector, tricking the human brain into perceiving deep 3D physical space without the massive GPU overhead of WebGL.

### 💻 Multi-Threaded Syntax Simulator

Located in `src/pages/About.jsx`, this infinite-looping code terminal simulates real-time typing. It seamlessly rotates through complex architectural snippets across Advanced Java, Spring Boot, React, and SQL, complete with a custom regex-based syntax highlighter.

### 🌗 Sub-Second State Transition (Cyber-Curtain)

A flawlessly executed Dark/Light mode switcher (`src/components/ThemeToggle.jsx`).

* To prevent the dreaded "CSS color flash", this component deploys a hardware-accelerated, full-screen circular clipping mask (`SYSTEM_REBOOT`) to seamlessly hide the DOM variable swap behind a cinematic animation.

---

## ⚡ PERFORMANCE OPTIMIZATION

This system is engineered for maximum frame rates.

* **GPU Acceleration:** All heavy animations, background blurs, and sweeping gradients are forced onto the GPU using `transform-gpu` and `will-change`.
* **Mobile-First Stripping:** Computationally expensive CSS properties (like `mix-blend-difference`) are dynamically stripped from mobile viewports to guarantee 60FPS scrolling on all handheld devices.

---

## ⚙️ DEPLOYMENT PROTOCOL

To instantiate this environment locally, execute the following boot sequence:

```bash
# 1. Clone the repository into local memory
git clone [https://github.com/Koustav2303/koustavpan-portfolio.git](https://github.com/Koustav2303/koustavpan-portfolio.git)

# 2. Access the root directory
cd koustavpan-portfolio

# 3. Install required node dependencies
npm install

# 4. Ignite the Vite development server
npm run dev

```

### 📡 Server Uplink (GitHub Pages)

This repository is pre-configured with the `gh-pages` deployment package. To push local changes to the live domain:

```bash
git add .
git commit -m "System update"
git push origin main
npm run deploy

```

---

## 👨‍💻 ABOUT THE ARCHITECT

**Koustav Pan**
*Java Fullstack Developer | Frontend Specialist* | *Bengaluru, India*

I bridge the gap between robust, scalable backend microservices and stunning, high-performance frontend interfaces. I don't just write code; I compile visions into deployable reality.

* **GitHub:** [@Koustav2303](https://github.com/Koustav2303)

> *"Any fool can write code that a computer can understand. Good programmers write code that humans can understand."* – Martin Fowler

```

```